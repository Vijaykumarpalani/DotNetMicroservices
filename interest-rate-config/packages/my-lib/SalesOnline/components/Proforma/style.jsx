import { styled } from '@mui/material';

export const StyledProformaDetails = styled('div')`
    width: 100%;
    padding-top: 2rem;
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    grid-auto-rows: auto;
    grid-auto-columns: auto;
    gap: 10rem;
`;

import { styled } from '@mui/material';

export const StyledAccess = styled('div')`

`;

export const StyledAccessRow = styled('div')`
    width: 70%;
    padding-top: 2rem;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    grid-auto-rows: auto;
    grid-auto-columns: auto;
    gap: 3rem;
`;